server:
  port: 8001

spring:
  application:
    name: fraud-service
  datasource:
    username: 'luv4code'
    password: 'postgres'
    url: jdbc:postgresql://localhost:5432/fraud
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
